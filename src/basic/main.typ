#import "../template.typ": *

- 使用 `std` 命名空间。
- 使用关闭流同步的 `cin` 和 `cout` 进行 IO。
- 用大写字母 $N,M$ 表示数据规模，定义时直接 +9。

#source("template.cpp")

== 预编译头文件

```bash
g++ stdc++.h -g -std=c++17 -O2
```

== VS Code 配置

TBD

== Tricks
#include "tricks.typ"